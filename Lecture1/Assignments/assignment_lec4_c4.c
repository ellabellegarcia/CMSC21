#include <stdio.h>

int main(void)                              // Uncapitalize V
{                                           // Missing bracket
    int sum;                                // Uncapitalize INT
    /* COMPUTE RESULT */                    // Close the comment
    sum = 25 + 37 - 19;                     // Missing semi-colon
    /* DISPLAY RESULTS */                   // Comment must end with */
    printf ("The answer is %i\n", sum);     // Separate with comma

    return 0;
}
